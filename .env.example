# --- Database (SMM-8 — User Database & Backend Connection) ---
DATABASE_URL="postgresql://postgres:12345@localhost:5432/smatch_db?schema=public"

# --- App ---
PORT=3000
NODE_ENV=development

# NOTE: JWT_SECRET, REDIS_HOST/PORT, MATCHMAKING_SERVICE_URL intentionally
# omitted here — they belong to other Sprint 1 tickets (Auth = SMM-6,
# Matchmaking Service Setup = SMM-7) which are not part of this file set.
